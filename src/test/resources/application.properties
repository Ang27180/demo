# Evita ejecutar schema.sql (MySQL) en tests si no hay la misma BD.
spring.sql.init.mode=never

# Garantiza JavaMailSender en @SpringBootTest (perfil default sin application-test).
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test-context@example.com
spring.mail.password=unused
